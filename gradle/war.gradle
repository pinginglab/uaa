apply plugin: "war"

bootWar {
    mainClassName = "com.pingsec.dev.UaaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
